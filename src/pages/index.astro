---
import { defaultLang } from '../i18n/config';

// Redirect to default language only if we're at the root
const url = new URL(Astro.request.url);
if (url.pathname === '/' || url.pathname === '') {
  return Astro.redirect(`/${defaultLang}`, 301);
}
---