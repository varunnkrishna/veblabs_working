---
---

<section class="relative overflow-hidden py-20">
    
    <!-- Gradient Background -->
    <div class="absolute inset-0 bg-gradient-to-br from-purple-50 to-blue-50 dark:from-purple-950/40 dark:to-blue-950/40"></div>
    
    <!-- Animated Shapes -->
    <div class="absolute inset-0">
      <div class="absolute top-10 left-10 w-72 h-72 bg-purple-200/30 dark:bg-purple-600/10 rounded-full mix-blend-multiply filter blur-xl animate-blob"></div>
      <div class="absolute top-0 right-4 w-72 h-72 bg-blue-200/30 dark:bg-blue-600/10 rounded-full mix-blend-multiply filter blur-xl animate-blob animation-delay-2000"></div>
      <div class="absolute -bottom-8 left-20 w-72 h-72 bg-pink-200/30 dark:bg-pink-600/10 rounded-full mix-blend-multiply filter blur-xl animate-blob animation-delay-4000"></div>
    </div>
    
  
    <div class="relative max-w-6xl mx-auto px-4 sm:px-6 lg:px-6">
      <div class="bg-white/70 dark:bg-gray-900/70 backdrop-blur-lg rounded-3xl p-8 md:p-12 shadow-[0_20px_50px_-20px_rgba(0,0,0,0.15)] dark:shadow-[0_20px_50px_-20px_rgba(0,0,0,0.5)] border border-gray-200/50 dark:border-gray-700/50 mx-auto">
        <h2 class="text-4xl md:text-4xl tracking-tighter text-center font-bold text-transparent bg-clip-text bg-gradient-to-r from-blue-950 to-blue-600 dark:from-purple-400 dark:to-blue-400 mb-6 leading-tight">
          Why Settle for Half-Baked Websites?
        </h2>
        
        <div class="relative">
          <p class="text-lg md:text-lg text-center text-gray-700 dark:text-gray-300 leading-relaxed mb-8 pl-4 max-w-4xl mx-auto">
            Imagine paying premium prices for a dish at a fine restaurant, only to find it's bland and poorly plated. Frustrating, right?

               Many developers and agencies deliver websites just like that—lacking the essentials like SEO,
              responsive design, and proper functionality.
            
          </p>
        </div>
  <div class="text-center">
        <button class="group relative inline-flex items-center justify-center px-8 py-4 font-bold text-white transition-all duration-200 bg-gradient-to-r from-blue-900 to-blue-600 rounded-xl hover:from-blue-700 hover:to-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-purple-500">
          Let's Fix It
          <svg class="w-5 h-5 ml-2 -mr-1 transition-transform duration-200 transform group-hover:translate-x-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
          </svg>
          <div class="absolute -inset-0.5 bg-gradient-to-r from-blue-950 to-blue-600 rounded-xl blur opacity-30 group-hover:opacity-50 transition duration-200"></div>
        </button>
        </div>
      </div>
    </div>
  </section>
  
  <style>
    @keyframes blob {
      0% { transform: translate(0px, 0px) scale(1); }
      33% { transform: translate(30px, -50px) scale(1.1); }
      66% { transform: translate(-20px, 20px) scale(0.9); }
      100% { transform: translate(0px, 0px) scale(1); }
    }
  
    .animate-blob {
      animation: blob 7s infinite;
    }
  
    .animation-delay-2000 {
      animation-delay: 2s;
    }
  
    .animation-delay-4000 {
      animation-delay: 4s;
    }
  </style>